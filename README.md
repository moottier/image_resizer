usage: resizer.py [-h] --sizes SIZES [SIZES ...] img_paths [img_paths ...]

Resize images

positional arguments:
  img_paths

optional arguments:
  -h, --help            show this help message and exit
  --sizes SIZES [SIZES ...], -s SIZES [SIZES ...]